# Security model

Governed path controls:

- deny/allow/ask policy engine;
- capability tokens for scoped actions;
- context firewall for untrusted tool output;
- deterministic flows for known business processes;
- audit traces for every safe run;
- reviewed lessons and CI guardrails.
